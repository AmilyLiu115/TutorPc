'use strict';

var home = new Vue({
    el: '#educDetail',
    data: function data() {
        return {
            searchSelect: '',
            searchText: '',
            indictor: 2,
            searchSelectArr: [{ name: '老师' }, { name: '学校' }],
            showActive: false
        };
    },

    methods: {
        selectItem: function selectItem(item, index) {
            this.searchSelectArr.splice(index, 1);
            this.searchSelectArr.unshift(item);
        },
        addClass: function addClass(index) {
            if (index == this.searchSelectArr.length - 1) {
                this.showActive = true;
            }
        }
    },
    created: function created() {}
});
//# sourceMappingURL=maps/educDetail.min.js.map
